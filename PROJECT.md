# macOS Task Scheduler for Humans

## Purpose
A human-friendly macOS task scheduler built on top of Apple's `launchd`. The goal is to make scheduled jobs easier to create, understand, test, and troubleshoot without requiring users to manually write plist files or memorize `launchctl` commands.

## Architecture
- **Language:** Python 3.12+
- **GUI:** PySide6 / Qt Widgets
- **CLI:** Typer
- **Testing:** pytest + pytest-cov
- **Linting:** Ruff + mypy
- **Domain Model:** Pydantic 2.x
- **plist handling:** Python standard library `plistlib`
- **Platform:** macOS (user LaunchAgents; system LaunchDaemons planned for Run phase on macOS 13+)
- **Packaging:** PySide6 deployment tooling → macOS `.app` bundle

### Architectural Layers
```
GUI (PySide6)          CLI (Typer)
        └──────── Application Services
                       Domain (Job/Schedule/Command/Environment)
              Platform Adapters (macOS plist/launchctl/filesystem)
                       launchd
```

## Status
**Version:** 0.0.42
**Phase:** Run (planned). Complete (v0.0.32): application debug logging and crash diagnostics — the app writes its own size-rotating debug log (`~/Library/Logs/macOS Task Scheduler for Humans/app.log`, DEBUG, ~1 MB × 3 backups) and captures every unhandled exception with its full traceback, plus background "unraisable" failures and Qt C++-side `qWarning`/`qCritical`/`qFatal` messages; the GUI shows a dialog naming the log file and then exits on an unexpected error, and `mactask` records command failures in the same log. Complete (v0.0.31): new-task identity polish — the Identity **Name** field is forced to lowercase as typed (to match the derived label) and the Name + read-only Label fields are widened (~15 → ~30 chars) for readability. Complete (v0.0.30): new-task identity clarity — the New Task and Edit Task dialogs present one editable **Name** field; the **Label** is read-only and auto-fills from the name (an existing job's fixed label is shown), so a human no longer copy/pastes the same text into both fields. Complete (v0.0.29): durable external enable/disable — Disable/Enable for an external LaunchAgent now writes the plist's `Disabled` key (the durable, human-visible source of truth) through the staged replace with a retained byte-identical backup, then aligns launchd alongside (`launchctl disable`/`enable` updates the override store; bootout when loaded on disable, bootstrap when not loaded on enable) so the state survives restarts and the **State** column reads it from the file; no-label disables still quarantine. Complete (v0.0.28): truthful task state display — the table **State** column and Overview **Enabled** row resolve enabled/disabled for external agents (parsed job flag first, then the plist's `Disabled` key, launchd-enabled default when absent) instead of dashes; unscheduled agents show **at login** (RunAtLoad only) or **not scheduled** in **Schedule**; **Status unknown** only when nothing can be determined. Complete (v0.0.27, shipped 2026-09-13): Universal Task Controls — every row in the task list supports create/edit/disable/remove in any and all states (managed saved/installed, external supported/partial/invalid, with or without a usable launchd label); structured edits rewrite only changed settings and preserve every other plist key, unrepresentable plists open in a raw plist editor, label-less disables quarantine the plist, removals are verified with a retained byte-identical backup — a wider amendment of the read-only-external policy, documented in the README/architecture docs. Walk complete: all 23 increments shipped (Crawl 0–13; Walk 14–23 at v0.0.23). Run phase plan approved 2026-09-09: increments 24–29 covering Run architecture/threat model/helper contract (24), scope-aware managed-job model (25), native Swift XPC helper + authenticated IPC (26), system LaunchDaemon lifecycle (27), system-service UX/CLI (28), and release security/distribution (29). Next increment: 24. The main GUI starts at an adaptive `1280x900` size bounded to usable display space, and Diagnostics, Persisted logs, and Python interpreter details begin collapsed so Overview is readable (v0.0.26). Increments 14 (schema-v2 schedule variants + v1→v2 migration), 15 (next-run preview), 16 (multi-time calendar authoring), 17 (interval and login triggers), 18 (Python environment detectors), and 19 (expanded diagnostics: typed diagnostic models, report engine with pinned group order, platform probes for protected paths and Mach-O architecture, GUI/CLI diagnostics surfacing) are complete (v0.0.19). Increment 20 (application-observed execution history: append-only sqlite3 history, service-boundary event recording, `mactask history`, GUI history panel) is complete (v0.0.20); increment 21 (external plist import) is complete (v0.0.21); increment 22 (Walk UX + catalog-only managed-JSON transfer: filterable/badged/empty-state task list, GUI/CLI `export-json`/`import-json` with identity-preserving create-only transfer and v1→v2 migration, and a platform-isolated Finder reveal) is complete (v0.0.22). Increment 23 (the four Python ecosystem detectors deferred from increment 18: pyenv, Conda, Pipenv, and Homebrew Python) is complete (v0.0.23).

**Current work:** In progress (v0.0.43): stability-first structured logging and error handling — durable JSONL audit log of every UI action/configuration change with full configuration values (no redaction), bounded 10 MB / 14-day retention replacing size-only rotation, typed terminal worker results so failures never leave the app stuck busy, responsive shutdown without blocking `QThread.wait`, and file-operation hardening so expected filesystem failures become user-visible outcomes. Complete (v0.0.42): shutdown SIGSEGV fix — `MainWindow`'s three worker `QThread`s (external-control, lifecycle, diagnostics) are now parentless (`QThread()`, not `QThread(self)`) so the window's destructor can never free a thread whose queued `deleteLater` is still pending; regression test records every dispatched thread through a recording proxy and exercises all three dispatch sites for real. Test/code ratio trim (formatting-only) back to 74.95%. Complete (v0.0.41): job-editor label kept on a single line — the New Task / Edit Task dialog's **Label** row is now a single-line plain-text display (a `QLabel` that never wraps) instead of the v0.0.40 word-wrapping one, because the wrapped label was clipped vertically and unreadable in its `QFormLayout` row; the name is still the only identity input, and typing the name still auto-fills the derived label live (an existing job's fixed label is shown). Complete (v0.0.40): job-editor label as plain text — the New Task / Edit Task dialog's **Label** row is now a word-wrapping plain-text display (`QLabel`) instead of a read-only input box, so it no longer reads as something the user should type into; the name is still the only identity input, and typing the name still auto-fills the derived label live (an existing job's fixed label is shown). Complete (v0.0.39): diagnostics/history panels hidden by default — the collapsed **Diagnostics** and **History** panel headers at the bottom of the right pane are hidden at startup so the right pane is just the task inspector; checkable **Show Diagnostics** / **Show History** items in the **View** menu (File, Edit, View, Actions, Diagnostics, Lifecycle) toggle each panel's visibility; the panels keep their existing behavior (test outcomes and log refreshes expand the Diagnostics panel; history renders on selection and expands on demand) and re-showing restores each panel with its current contents; the preference is not persisted — the panels start hidden in every session.

## Repository
Source of truth: https://github.com/JohnHoaglun/macOS-Task-Scheduler-for-Humans/tree/sched_dev_opencode
Branch: `sched_dev_opencode`

## Credentials
N/A - no credentials required
