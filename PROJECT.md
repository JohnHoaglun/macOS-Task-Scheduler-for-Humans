# macOS Task Scheduler for Humans

## Purpose
A human-friendly macOS task scheduler built on top of Apple's `launchd`. The goal is to make scheduled jobs easier to create, understand, test, and troubleshoot without requiring users to manually write plist files or memorize `launchctl` commands.

## Architecture
- **Language:** Python 3.12+
- **GUI:** PySide6 / Qt Widgets
- **CLI:** Typer
- **Testing:** pytest + pytest-cov
- **Linting:** Ruff + mypy
- **Domain Model:** Pydantic 2.x
- **plist handling:** Python standard library `plistlib`
- **Platform:** macOS (user LaunchAgents; system LaunchDaemons planned for Run phase on macOS 13+)
- **Packaging:** PySide6 deployment tooling → macOS `.app` bundle

### Architectural Layers
```
GUI (PySide6)          CLI (Typer)
        └──────── Application Services
                       Domain (Job/Schedule/Command/Environment)
              Platform Adapters (macOS plist/launchctl/filesystem)
                       launchd
```

## Status
**Version:** 0.0.50
**Phase:** Run (planned). Complete (v0.0.50): Round-2 review remediation, slice 3 (GUI responsiveness and presentation) — asynchronous catalog discovery and raw-editor reads moved off the GUI thread with coalesced refresh and close-drain registration, a 256 KiB bounded log tail with a truncation marker (GUI and CLI), signal-blocked draft loading so interpreter detection no longer probes after every editor open, shared `shlex`-quoted command and `%H:%M` schedule formatting across GUI and CLI, and per-row filter dimension/search caching. Complete (v0.0.49): Round-2 review remediation, slice 2 (catalog, history, trust, CLI) — fault-tolerant catalog scan with surfaced unreadable-file diagnostics (CLI stderr + GUI status bar), 1,000-event-per-job execution-history retention pruned on open and after each append, `save()` under the catalog lock, the CLI error matrix (narrow exception catches, exit 2 reserved for usage, non-empty stderr, no tracebacks), symlink rejection at plist discovery and read, `HistoryTableModel.header` bounds check, and preview-time SHA-256/device/inode re-verification so an external import whose source drifts between preview and commit is refused. Complete (v0.0.48): Round-2 review remediation, slice 1 (safety and observability) — explicit label-invariant enforcement for label-less raw edits, confirmed-not-loaded bootout recovery for uninstall/reinstall with staged-sibling cleanup, a 30-second deadline and explicit UTF-8 decoding for every `launchctl` subprocess, a `Label`/path consistency check on `bootstrap_path`, worker-failure logging with terminal status messages, once-only history append-failure disclosure, a GUI-thread-marshaled crash dialog with `faulthandler` capture and abort-on-`qFatal`, and removal of dead telemetry/backup paths. Complete (v0.0.47): filesystem safety, packaging portability, and editor debounce — secure exclusive temp-file creation, descriptor-coherent snapshots with best-effort verified ops, snapshot-based quarantine, a portable packaging spec, debounced Python detection, and explicit macOS package prerequisites. Complete (v0.0.46): logging resilience and security — failure-tolerant `configure_logging()` with a structured JSONL stderr fallback, `logging_degraded_reason()` status, verified `0600` permissions, active-file retention, one-attempt write recovery, path-aware reconfiguration, degraded GUI disclosure, degraded-safe crash wording, and documented full-configuration logging policy. Complete (v0.0.45): durable JSON catalog/export writes, race-safe create-only import/export, and safe import/history controller failure outcomes. Complete (v0.0.44): approved code-review remediation — release-quality gates, plain-text raw-plist inspection, fail-closed external removal, phase-truthful external messages, and direct-test close-drain registration. Complete (v0.0.43): stability-first structured logging and error handling — durable JSONL audit log of every UI action/configuration change with full configuration values (no redaction), bounded 10 MB / 14-day retention, typed terminal worker results, safe worker-thread lifetime, and responsive shutdown. Complete (v0.0.32): application debug logging and crash diagnostics — the app writes its own size-rotating debug log (`~/Library/Logs/macOS Task Scheduler for Humans/app.log`, DEBUG, ~1 MB × 3 backups) and captures every unhandled exception with its full traceback, plus background "unraisable" failures and Qt C++-side `qWarning`/`qCritical`/`qFatal` messages; the GUI shows a dialog naming the log file and then exits on an unexpected error, and `mactask` records command failures in the same log. Complete (v0.0.31): new-task identity polish — the Identity **Name** field is forced to lowercase as typed (to match the derived label) and the Name + read-only Label fields are widened (~15 → ~30 chars) for readability. Complete (v0.0.30): new-task identity clarity — the New Task and Edit Task dialogs present one editable **Name** field; the **Label** is read-only and auto-fills from the name (an existing job's fixed label is shown), so a human no longer copy/pastes the same text into both fields. Complete (v0.0.29): durable external enable/disable — Disable/Enable for an external LaunchAgent now writes the plist's `Disabled` key (the durable, human-visible source of truth) through the staged replace with a retained byte-identical backup, then aligns launchd alongside (`launchctl disable`/`enable` updates the override store; bootout when loaded on disable, bootstrap when not loaded on enable) so the state survives restarts and the **State** column reads it from the file; no-label disables still quarantine. Complete (v0.0.28): truthful task state display — the table **State** column and Overview **Enabled** row resolve enabled/disabled for external agents (parsed job flag first, then the plist's `Disabled` key, launchd-enabled default when absent) instead of dashes; unscheduled agents show **at login** (RunAtLoad only) or **not scheduled** in **Schedule**; **Status unknown** only when nothing can be determined. Complete (v0.0.27, shipped 2026-09-13): Universal Task Controls — every row in the task list supports create/edit/disable/remove in any and all states (managed saved/installed, external supported/partial/invalid, with or without a usable launchd label); structured edits rewrite only changed settings and preserve every other plist key, unrepresentable plists open in a raw plist editor, label-less disables quarantine the plist, removals are verified with a retained byte-identical backup — a wider amendment of the read-only-external policy, documented in the README/architecture docs. Walk complete: all 23 increments shipped (Crawl 0–13; Walk 14–23 at v0.0.23). Run phase plan approved 2026-09-09: increments 24–29 covering Run architecture/threat model/helper contract (24), scope-aware managed-job model (25), native Swift XPC helper + authenticated IPC (26), system LaunchDaemon lifecycle (27), system-service UX/CLI (28), and release security/distribution (29). Next increment: 24. The main GUI starts at an adaptive `1280x900` size bounded to usable display space, and Diagnostics, Persisted logs, and Python interpreter details begin collapsed so Overview is readable (v0.0.26). Increments 14 (schema-v2 schedule variants + v1→v2 migration), 15 (next-run preview), 16 (multi-time calendar authoring), 17 (interval and login triggers), 18 (Python environment detectors), and 19 (expanded diagnostics: typed diagnostic models, report engine with pinned group order, platform probes for protected paths and Mach-O architecture, GUI/CLI diagnostics surfacing) are complete (v0.0.19). Increment 20 (application-observed execution history: append-only sqlite3 history, service-boundary event recording, `mactask history`, GUI history panel) is complete (v0.0.20); increment 21 (external plist import) is complete (v0.0.21); increment 22 (Walk UX + catalog-only managed-JSON transfer: filterable/badged/empty-state task list, GUI/CLI `export-json`/`import-json` with identity-preserving create-only transfer and v1→v2 migration, and a platform-isolated Finder reveal) is complete (v0.0.22). Increment 23 (the four Python ecosystem detectors deferred from increment 18: pyenv, Conda, Pipenv, and Homebrew Python) is complete (v0.0.23).

**Current work:** Complete (v0.0.50): approved Round-2 review remediation slice 3 (GUI responsiveness and presentation) — R2-03 asynchronous `DiscoveryWorker` with coalesced `MainWindow.refresh()` + close-drain registration, R2-08 256 KiB `LOG_TAIL_BYTES` log tail with `truncated`/`total_bytes` DTO + worker-backed GUI reads + CLI tail marker, R2-18 `QSignalBlocker`-wrapped `_load_draft` (no post-open interpreter probe), R2-19 shared `domain/formatting.py` helpers (`quote_argv`, `format_command_argv`, `format_schedule_text`, `format_truncation_marker`) adopted across the presenter, import preview, CLI renderer, and search haystack, R2-21 per-row `_RowView` cache in `AgentTableModel.set_agents` with O(1) `data()`, plus a ratio-trim closeout (multi-line test-signature collapse across 18 files, parameterized merges, and dead preview-constant / empty-class removal) to hold the 75% ratio cap. Final gate: `make check` clean (ruff + mypy strict + 741 tests, 100% coverage); test/source ratio 74.9036% (11,070 test : 14,779 src, under the 75% cap); version 0.0.49 → 0.0.50 (all 4 registry locations). Complete (v0.0.49): approved Round-2 review remediation slice 2 (catalog, history, trust, CLI) — R2-04 fault-tolerant catalog scan (`CatalogDiagnostic`, `catalog_diagnostics()`; `list` diagnostics to stderr; GUI status bar via `RefreshOutcome.diagnostics`), R2-07 `MAX_EVENTS_PER_JOB = 1000` retention pruned on open and after append, R2-11 `save()` under `.catalog.lock`, R2-14 CLI error matrix (narrow catches; exit 2 reserved for usage; non-empty stderr; `logs` stream errors exit 1), R2-16 symlink rejection in `list_plist_files`/`read_plist_bytes`, R2-20 `HistoryTableModel.header` bounds check, R2-22 import preview snapshot (SHA-256 + dev/ino) re-verified at commit with `ValueError` → exit 1, plus a ratio-trim closeout (banner comments, import-statement collapse, docstring removals, line-redundant test cuts) to hold the 75% ratio cap. Final gate: `make check` clean (ruff + mypy strict + 690 tests, 100% coverage); test/source ratio 74.5787% (10,755 test : 14,421 src, under the 75% cap); version 0.0.48 → 0.0.49 (all 4 registry locations). Complete (v0.0.48): approved Round-2 review remediation slice 1 (safety and observability) — R2-01 explicit label-invariant `ValueError` for label-less raw edits, R2-02 worker-failure logging + terminal status messages, R2-05 confirmed-not-loaded uninstall/reinstall recovery with fail-closed staged-sibling cleanup, R2-06 abort-on-`qFatal`, R2-09 dead telemetry API deletion, R2-10 30-second subprocess deadline + UTF-8 replace decode, R2-12 once-only history append-failure disclosure, R2-13 `faulthandler` + GUI-thread-marshaled crash dialog, R2-15 narrow `(InvalidFileException, ValueError)` plist catches, R2-17 `bootstrap_path` Label check, R2-24 dead `backup_external` deletion, R2-25 documented CLI degraded-output caveat, plus a line-redundant test trim to hold the 75% ratio cap. Final gate: `make check` clean (ruff + mypy strict + 649 tests, 100% coverage); test/source ratio 74.6273% (10,662 test : 14,287 src, under the 75% cap); version 0.0.47 → 0.0.48 (all 4 registry locations). Complete (v0.0.47): approved slice for the remaining code-review findings — secure exclusive temp-file creation (CR-08), descriptor-coherent snapshots with honest best-effort verified-operation semantics (CR-09), snapshot-based external quarantine (CR-16), portable packaging configuration (CR-12), debounced Python detection (CR-17), and explicit macOS package prerequisites (CR-21). Final gate: `make check` clean (ruff + mypy strict + 654 tests, 100% coverage); test/source ratio 74.9665% (10,640 test : 14,193 src, under the 75% cap); version 0.0.46 → 0.0.47 (all 4 registry locations). Complete (v0.0.46): failure-tolerant application logging with a structured JSONL stderr fallback, `logging_degraded_reason()` status, verified `0600` permissions, active-file retention, one-attempt write recovery, path-aware reconfiguration, degraded GUI disclosure, degraded-safe crash wording, and documented full-configuration logging policy. Final gate: `make check` clean (ruff + mypy strict + 645 tests, 100% coverage); test/source ratio 74.9716% (10,556 test : 14,080 src, under the 75% cap); version 0.0.45 → 0.0.46 (all 4 registry locations). Complete (v0.0.45): durable atomic JSON catalog/export writes, serialized create-only catalog import, create-only managed-JSON export, and safe `OSError` outcomes in import/history controllers. Final gate: `make check` clean (ruff + mypy strict + 628 tests, 100% coverage); test/source ratio 74.8702% (10,383 test : 13,868 src, under the 75% cap); version 0.0.44 → 0.0.45 (all 4 registry locations). Complete (v0.0.44): approved code-review remediation — `make check` release-quality gates, plain-text raw-plist inspection, fail-closed external removal after failed bootout, phase-truthful external enable/disable/remove messages, and direct-test worker registration in the main-window close drain. Final gate: `make check` clean (ruff + mypy strict + 621 tests, 100% coverage); test/source ratio 74.3196% (10,268 test : 13,816 src, under the 75% cap); version 0.0.43 → 0.0.44 (all 4 registry locations). Complete (v0.0.43): stability-first structured logging and error handling — durable JSONL audit log of every UI action/configuration change with full configuration values (no redaction), bounded 10 MB / 14-day retention replacing size-only rotation, typed terminal worker results so failures never leave the app stuck busy, safe worker-thread lifetime, responsive shutdown without blocking `QThread.wait`, and file-operation hardening so expected filesystem failures become user-visible outcomes. Final gate: `make check` clean (ruff + mypy strict + 624 tests, 100% coverage); test/source ratio 74.6089% (10,255 test : 13,745 src, under the 75% cap); version 0.0.42 → 0.0.43 (all 4 registry locations). Complete (v0.0.42): shutdown SIGSEGV fix — `MainWindow`'s three worker `QThread`s (external-control, lifecycle, diagnostics) are now parentless (`QThread()`, not `QThread(self)`) so the window's destructor can never free a thread whose queued `deleteLater` is still pending; regression test records every dispatched thread through a recording proxy and exercises all three dispatch sites for real. Test/code ratio trim (formatting-only) back to 74.95%. Complete (v0.0.41): job-editor label kept on a single line — the New Task / Edit Task dialog's **Label** row is now a single-line plain-text display (a `QLabel` that never wraps) instead of the v0.0.40 word-wrapping one, because the wrapped label was clipped vertically and unreadable in its `QFormLayout` row; the name is still the only identity input, and typing the name still auto-fills the derived label live (an existing job's fixed label is shown). Complete (v0.0.40): job-editor label as plain text — the New Task / Edit Task dialog's **Label** row is now a word-wrapping plain-text display (`QLabel`) instead of a read-only input box, so it no longer reads as something the user should type into; the name is still the only identity input, and typing the name still auto-fills the derived label live (an existing job's fixed label is shown). Complete (v0.0.39): diagnostics/history panels hidden by default — the collapsed **Diagnostics** and **History** panel headers at the bottom of the right pane are hidden at startup so the right pane is just the task inspector; checkable **Show Diagnostics** / **Show History** items in the **View** menu (File, Edit, View, Actions, Diagnostics, Lifecycle) toggle each panel's visibility; the panels keep their existing behavior (test outcomes and log refreshes expand the Diagnostics panel; history renders on selection and expands on demand) and re-showing restores each panel with its current contents; the preference is not persisted — the panels start hidden in every session.

## Repository
Source of truth: https://github.com/JohnHoaglun/macOS-Task-Scheduler-for-Humans/tree/sched_dev_opencode
Branch: `sched_dev_opencode`

## Credentials
N/A - no credentials required
